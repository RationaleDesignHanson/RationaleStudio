# ============================================
# FIREBASE CONFIGURATION
# ============================================

# Firebase Client Configuration for Rationale Studio Website
# Get these values from Firebase Console: Project Settings > General
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Firebase Admin SDK (Server-side Authentication)
# Download serviceAccountKey.json from Firebase Console: Project Settings > Service Accounts
# This enables protected routes and user management
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# ============================================
# EXTERNAL API INTEGRATIONS
# ============================================

# GitHub API (Team Portal - Repository Integration)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org
GITHUB_TOKEN=ghp_your_github_personal_access_token
GITHUB_ORG=your_github_organization

# Figma API (Admin/Design System)
# Create at: https://www.figma.com/developers/api#access-tokens
FIGMA_ACCESS_TOKEN=figd_your_figma_access_token
FIGMA_FILE_KEY=your_figma_file_key

# Notion API (Documentation & Content)
# Create at: https://www.notion.so/my-integrations
NOTION_API_KEY=ntn_your_notion_api_key

# Linear API (Project Tracking)
# Create at: https://linear.app/settings/api
LINEAR_API_KEY=lin_api_your_linear_api_key
LINEAR_TEAM_ID=your_team_id

# ============================================
# OUTBOUND PITCH SYSTEM
# ============================================

# Secure token generation for pitch pages
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PITCH_TOKEN_SECRET=your_64_character_hex_string

# Base URL for pitch link generation
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Resend API (Email Delivery)
# Create at: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key

# ============================================
# SUPABASE (Database & Storage)
# ============================================

# Get these values from Supabase Dashboard: Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your_project_ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Fill in all the values above with your actual credentials
#
# 3. Never commit .env.local to git (it's in .gitignore)
#
# 4. For production deployment:
#    - Set all environment variables in Netlify Dashboard
#    - Or use: netlify env:set KEY_NAME "value"
#
# 5. Required for basic functionality:
#    - Firebase configuration (all NEXT_PUBLIC_FIREBASE_* and FIREBASE_SERVICE_ACCOUNT_PATH)
#    - Supabase configuration (all SUPABASE_* variables)
#
# 6. Optional integrations (only needed if using those features):
#    - GitHub API (team portal repository integration)
#    - Figma API (design system access)
#    - Notion API (documentation integration)
#    - Linear API (project tracking)
#    - Resend API (email delivery for pitch system)
#
