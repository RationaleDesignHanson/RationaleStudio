# Credential Protection Pre-Commit Hook
# Prevents accidental commits of sensitive credentials

echo "🔒 Checking for credentials in staged files..."

# Filenames to block completely.
#
# These are GLOBS matched against the basename, not regexes. They used to be
# fed to `grep -E`, where a leading `*` is a repetition operator with nothing
# to repeat: grep exited 2 with "repetition-operator operand invalid", the `if`
# read that as no-match, and the four wildcard rules below silently enforced
# NOTHING. A private key staged as key.pem was waved straight through — only
# the separate content scan for "BEGIN ... PRIVATE KEY" was catching those, and
# only when the file happened to be PEM-encoded text.
#
# Glob matching also makes `.` literal. As a regex, ".env.local" matched
# "xenvxlocal" and, being unanchored, any path merely containing it.
BLOCKED_FILES=(
  "serviceAccountKey.json"
  "credentials.json"
  "firebase-credentials.json"
  ".firebase-credentials.json"
  ".env"
  ".env.*"        # .env.local, .env.production.local, .env.local.bak
  "*.pem"
  "*.key"
  "*.p12"
  "*.pfx"
)

# Suffixes that are meant to be committed. `.env.example` is tracked in this
# repo, so the .env.* rule above has to make room for it.
ALLOWED_SUFFIXES=(
  "*.example"
  "*.sample"
  "*.template"
)

# `set -f` stops the shell pathname-expanding the patterns against the working
# tree before `case` ever sees them; IFS pins splitting to newlines so a staged
# filename containing a space stays one filename.
OLD_IFS=$IFS
set -f
IFS='
'
BLOCKED_HIT=""
for file in $(git diff --cached --name-only); do
  base=${file##*/}

  allowed=""
  for ok in "${ALLOWED_SUFFIXES[@]}"; do
    case "$base" in $ok) allowed=1 ;; esac
  done
  [ -n "$allowed" ] && continue

  for pattern in "${BLOCKED_FILES[@]}"; do
    case "$base" in
      $pattern)
        echo "❌ BLOCKED: Attempt to commit sensitive file: $file"
        echo "   Matched the rule: $pattern"
        BLOCKED_HIT=1
        ;;
    esac
  done
done
IFS=$OLD_IFS
set +f

if [ -n "$BLOCKED_HIT" ]; then
  echo "   These files should NEVER be committed to git."
  echo "   Remove them from staging with: git reset HEAD <file>"
  exit 1
fi

# Patterns to search for in file contents (common credential formats).
# Token bodies require enough trailing length (>=20) so substrings inside
# ordinary identifiers (e.g. captu*re_p*ageview) don't trip the hook.
# (Word-boundary \b is unreliable across BSD/GNU grep, so length is the
# discriminator instead.)
CREDENTIAL_PATTERNS=(
  # API Keys
  "ghp_[a-zA-Z0-9]{20,}"                  # GitHub Personal Access Token
  "figd_[a-zA-Z0-9_-]{20,}"               # Figma Access Token
  "ntn_[a-zA-Z0-9]{20,}"                  # Notion API Key
  "lin_api_[a-zA-Z0-9]{20,}"              # Linear API Key
  "re_[a-zA-Z0-9]{20,}"                   # Resend API Key
  "AIza[a-zA-Z0-9_-]{20,}"                # Google API Key (Firebase/Gemini)
  "sk-[a-zA-Z0-9]{20,}"                   # OpenAI API Key
  "sk-proj-[a-zA-Z0-9]{20,}"              # OpenAI Project Key
  "phx_[a-zA-Z0-9]{20,}"                  # PostHog Personal API Key (phc_ project keys are public, not blocked)

  # Private Keys
  "BEGIN.*PRIVATE KEY"                    # PEM format private keys
  "BEGIN RSA PRIVATE KEY"                 # RSA private keys
  "BEGIN EC PRIVATE KEY"                  # EC private keys

  # AWS Credentials
  "AKIA[0-9A-Z]{16,}"                     # AWS Access Key ID
  "aws_secret_access_key"                 # AWS secret

  # Generic Secrets — only flag actual assignments with token-length values.
  "API_KEY[[:space:]]*=[[:space:]]*[\"']?[a-zA-Z0-9_-]{20,}"
  "SECRET[[:space:]]*=[[:space:]]*[\"']?[a-zA-Z0-9_-]{20,}"
  "TOKEN[[:space:]]*=[[:space:]]*[\"']?[a-zA-Z0-9_-]{20,}"
)

# Get list of staged files (excluding package-lock.json and example files which can have false positives)
#
# The `|| true` is load-bearing. Husky runs this hook with `sh -e`, and grep
# exits 1 when it filters everything out — so staging only an .svg, a lockfile
# or an .example file killed the hook right here, with no message and a
# non-zero status. That reads as a blocked commit for no stated reason, and it
# also made the "No files to check" branch below unreachable.
STAGED_FILES=$(git diff --cached --name-only | grep -vE '(package-lock\.json|\.lock$|\.svg$|\.example$|\.sample$|\.template$|^\.husky/)' || true)

# Skip check if no files to check
if [ -z "$STAGED_FILES" ]; then
  echo "✅ No files to check"
  exit 0
fi

# Check each pattern in staged files
FOUND_ISSUE=0
for pattern in "${CREDENTIAL_PATTERNS[@]}"; do
  # Use -I to ignore binary files, -E for extended regex
  if echo "$STAGED_FILES" | xargs -r git diff --cached 2>/dev/null | grep -qE "$pattern" 2>/dev/null; then
    if [ $FOUND_ISSUE -eq 0 ]; then
      echo "❌ BLOCKED: Potential credentials detected in staged files!"
      echo ""
      FOUND_ISSUE=1
    fi
    # Don't print the actual pattern to avoid leaking info
    echo "   Suspicious pattern detected (see .husky/pre-commit for details)"
  fi
done

if [ $FOUND_ISSUE -eq 1 ]; then
  echo ""
  echo "🛡️  Security Check Failed!"
  echo "   Please review your changes and remove any credentials."
  echo "   If this is a false positive, you can bypass this check with:"
  echo "   git commit --no-verify"
  echo ""
  exit 1
fi

echo "✅ No credentials detected"

# Optional: Run tests (uncomment if you have tests configured)
# npm test
